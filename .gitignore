# Byte-compiled / caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pytype/
.pyre/

# Virtual environments
.venv/
.venv-*/
venv/
env/
ENV/

# Jupyter
.ipynb_checkpoints/
.ipynb_checkpoints

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
.env
.env.*

# Weights & Biases (if enabled)
wandb/

# Test / coverage
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
*.cover
.hypothesis/

# poster folder
poster/

# results/ — single tree: ignore checkpoints, logs, predictions, latents, model weights; keep tables, light figures, traces, export JSON
results/**/logs/**
results/**/checkpoints/**
results/**/predictions/**
results/**/latents/**
results/**/*.ckpt
results/**/*.pth
results/**/models/**/*.pt
results/**/traces/**


# NPZ caches / dumps (anywhere under results/)
results/**/*.npz

# Optional: skip large vector figures
results/**/*.pdf

# data_mngmt: large derived artifacts (scripts tracked; docs only in repo README)
data_mngmt/generated/
*data_mngmt/legacy/**

# Vendored by ``python -m data_mngmt.sepsis_rl.mimic_sepsis_vendor`` (MIT upstream; re-run to refresh)
data_mngmt/vendor/mimic_sepsis_upstream/

data_mngmt/mortality_for_coper.data
data_mngmt/generated/mortality_coper_*.data
data_mngmt/mortality_for_coper_mdp_legacy.data
# YerevaNN/mimic3-benchmarks (``python -m data_mngmt.mimic.mimic3_benchmarks_vendor``)
data_mngmt/vendor/mimic3_benchmarks/
# Legacy optional ICU-Sepsis CSV bundle (see paths.json)
data_mngmt/legacy/icu-sepsis-csv-tables.tar.gz
data_mngmt/legacy/icu-sepsis-csv-tables/
# Deprecated locations (keep ignoring if still present locally)
data_mngmt/icu-sepsis-csv-tables.tar.gz
data_mngmt/icu-sepsis-csv-tables/

# MDP rebuild outputs / local cohort tables (optional)
data_mngmt/mimic_mdp_params/
data_mngmt/cohort/

# Duplicate of icu_sepsis/icu_sepsis + icu_sepsis_helpers (upstream "packages/" layout)
icu_sepsis/packages/

# Redundant notebook; canonical copy is notebooks/icu_sepsis_demo.ipynb
icu_sepsis/demo.ipynb

# Legacy / mistaken notebook output dirs (canonical: results/demo_outputs/<notebook>/{figures,tables,...}/)
icu_sepsis/demo_outputs/
notebooks/demo_outputs/

# Local data / temp (from former icu_sepsis/.gitignore)
icu_sepsis/data/
temp/

# Policy experiment outputs (from policies/ workflows)
policies/results/
policies/plots/

# Misc
*.dw
